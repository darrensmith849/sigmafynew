# @sigmafy/emails

React Email templates + a Resend-backed `sendEmail()` helper.

## Phase -1

`sendEmail()` throws `"not implemented"`. One placeholder template
(`PlaceholderEmail`) exists so the build pipeline exercises React Email.

## Phase 0B (next)

Welcome email and topic-graded email, both sent via Resend.
