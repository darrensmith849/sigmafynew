[project]
name = "sigmafy-stats-api"
version = "0.0.0"
description = "Sigmafy FastAPI stats service. Phase -1 placeholder."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"
